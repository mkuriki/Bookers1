
<body>
    <h1>Books</h1>
    <table>
        <tr>
            <th>Title</th>
            <th>Body</th>
            <th colspan="3"></th>
        </tr>
        <tbody>
            <% @lists.each do |list| %>
                <tr>
                     <td><%= list.title %></td>
                     <td><%= list.body %></td>
                     <td><%= link_to "show", book_path(list.id) %></td>
                     <td><%= link_to "edit", edit_book_path(list.id) %></td>
                     <td><%= link_to "destroy", book_path(list.id) %></td>
                </tr>
            <% end %>
        </tbody>
    </table>
    
</body>